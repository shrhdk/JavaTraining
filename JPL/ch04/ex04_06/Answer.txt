(a) N分木のノードを表すTreeNode
    → N分木のノードを表すオブジェクトのためのTreeNode

	具象クラス → 抽象クラス


(b) 特定の順番(深さ優先、幅優先など)で木を探索するTreeWalker
    → 特定の順番(深さ優先、幅優先など)で木を探索するオブジェクトのためのTreeWalker

	具象クラス → 抽象クラス


(c) グラフィックシステムにより描画可能なオブジェクトのためのDrawable
    → グラフィックシステムにより描画可能なオブジェクト

	インタフェース → 具象クラス


(d) グラフィックデスクトップから実行できるプログラムのApplication
    → グラフィックデスクトップから実行できるプログラム

	抽象クラス → 具象クラス